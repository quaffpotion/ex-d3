Works:
python -m SimpleHTTPServer 8000 &
python -m http.server 8000 &
(& runs process in background, type fg / bg to change)

This also works:
gem install adsf
adsf
(default runs on 3000 and does load the data)
